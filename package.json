{
  "name": "resource-providers",
  "version": "0.0.1",
  "description": "Manifest generation for Azure Resource Providers",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf dist",
    "watch": "tsc -p . -w",
    "lint": "npx eslint src",
    "generate": "ts-node src/generate.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.2",
    "@typescript-eslint/parser": "^7.1.0",
    "eslint": "^8.57.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}